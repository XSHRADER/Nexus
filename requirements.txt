# NEXUS AI -- fully local. CPU-only except final generation (Ollama).
# Python 3.13 compatible pins.

# Retrieval stack
chromadb==1.5.5
sentence-transformers==5.1.2
rank-bm25==0.2.2

# Document loaders
pypdf==4.3.1
python-docx==1.1.2

# UI + HTTP
streamlit==1.62.0
requests==2.32.3
